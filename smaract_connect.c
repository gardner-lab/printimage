#include <mex.h>

void mexFunction(int nlhs, mxArray *plhs[], int nrhs, const mxArray *prhs[]) {
    
    // Does the named pipe exist?
    
    
    // Is there a server process on the other end of it?
    fwrite(named pipe
    mexPrintf("Hello, world! Got %d rhs args.\n", nrhs);
}
